# Core dependencies
schedule>=1.2.0

# Semantic memory search (find_related_memories.py)
# First run downloads model ~90MB; subsequent runs use local cache
sentence-transformers>=3.0.0
numpy>=1.24.0

# Optional: for voice synthesis (WSL/Windows)
# edge-tts>=6.1.0

# Optional: for Gmail integration
# google-api-python-client>=2.0.0
# google-auth-httplib2>=0.1.0
# google-auth-oauthlib>=1.0.0
